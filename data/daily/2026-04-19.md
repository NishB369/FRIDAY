# Daily Log — 2026-04-19

## Session 1 — Audio→karaoke skill + N2/N3a/N3c/N3d nodes + HTML previewer

- Built `audio-to-karaoke` skill: full audio → Sarvam+Groq dual transcribe → segment-anchored alignment → Hinglish via Claude Haiku → Remotion render → audio mux. Handles arbitrary length via 29s chunking.
- Ran end-to-end on `New Recording.m4a` (Christopher Marlowe + Doctor Faustus intro, 3:28). Renamed all artifacts to slug `christopher_marlowe_doctor_faustus_intro`.
- N2: `normalize_to_n2.py` — unified canonical JSON shape supporting both `source: audio` and `source: youtube` (optional youtube block).
- N3a: `summarize_n2.py` — generates overview (full + YAML frontmatter) then auto-splits into `summary/` (narrative) and `notes/` (analysis) views to match existing peer-file conventions. Dropped N3b as redundant.
- N3c: `generate_quiz_n2.py` — 6 Qs (4 MCQ + 2 T/F) from overview md via Claude Haiku. `--push` flag gated on `source=youtube`.
- N3d: `generate_yt_metadata_n2.py` — generates YT title/description/tags from scratch for audio-source flow (vs legacy `det_optimize`/`ai_optimize` which fix existing YT metadata).
- HTML previewer: `build_preview.py` scans `tlt/n2/`, emits `tlt/preview/{index.html, manifest.json}`. Tabs auto-derive from artifact registry — adding a node = one row in `ARTIFACTS`.
- PIPELINE.md updated: status table, dropped N3b, marked N0/N1/N2/N3a/N3c/N3d-audio + previewer as ✓; remaining N4-VIDEO extras, N4-WEB, N5, N6.
